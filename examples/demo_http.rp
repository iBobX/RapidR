' ── RapidR HTTP Demo ──
' Simple HTTP client demo using RHTTP
$APPTYPE CONSOLE

DIM http AS RHTTP

PRINT "Fetching httpbin.org/get..."
DIM result AS STRING
http.timeout = 5000
result = http.get("https://httpbin.org/get")

PRINT "Status: " + STR$(http.statuscode)
PRINT "Response:"
PRINT LEFT$(result, 500)
